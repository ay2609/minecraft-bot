---
phase: 05-llm-client-and-tactical-planner
plan: 04
type: execute
wave: 4
depends_on:
  - 05-03
files_modified:
  - src/index.ts
  - src/planner/TacticalPlanner.test.ts
  - src/index.planner-context.integration.test.ts
autonomous: false
gap_closure: true
requirements:
  - PLAN-01
  - PLAN-02
must_haves:
  truths:
    - "Runtime emits planner:context-ready from real perception:updated events after startup wiring (not only in isolated ContextAssembler tests)"
    - "TacticalPlanner receives a non-null assembled context payload before invoking FireworksLLMClient on executor-result cycles"
    - "On each executor:result cycle, LLM user payload includes context snapshot + memory fields instead of context:null"
    - "Live Fireworks call with valid FIREWORKS_API_KEY succeeds after gap fix and tactical:queue-ready emits a non-WAIT queue at least once"
    - "In-game simple goal progression (move_to + break_block) is observable without manual per-action intervention"
  artifacts:
    - path: "src/index.ts"
      provides: "ContextAssembler + planner-memory retriever runtime wiring and planner:context-ready emission"
      contains: "eventBus.on('perception:updated')"
    - path: "src/planner/TacticalPlanner.test.ts"
      provides: "Unit-level proof that LLM call payload context is non-null when planner:context-ready is supplied"
    - path: "src/index.planner-context.integration.test.ts"
      provides: "Startup integration proof that initializeApplication wiring produces planner:context-ready before tactical cycle"
  key_links:
    - from: "src/index.ts"
      to: "src/perception/ContextAssembler.ts"
      via: "assembleAndPublishPlannerContext"
      pattern: "planner:context-ready emission on perception:updated"
    - from: "src/index.ts"
      to: "src/memory/index.ts"
      via: "createPlannerMemoryRetriever"
      pattern: "retrieve semantic/episodic memory for planner context"
    - from: "src/planner/TacticalPlanner.ts"
      to: "src/events/EventBus.ts"
      via: "planner:context-ready -> latestContextBundle -> llmClient.call(messages)"
      pattern: "messages[1].content includes non-null context"
---

<objective>
Close Phase 05 verification gaps without replanning completed 05-01..05-03 scope: wire assembled planner context into the runtime event flow and add explicit coverage proving TacticalPlanner gets non-null context before LLM calls.

Purpose: Verification showed PLAN-02 is blocked because runtime never emits planner:context-ready, leaving TacticalPlanner context as null. PLAN-01 also remains uncertain for live API behavior because smoke verification must be re-run after the runtime fix.

Output: runtime context wiring in `src/index.ts`, integration/unit coverage for non-null context delivery, and a blocking human verification checkpoint to confirm live Fireworks + in-game progression.
</objective>

<execution_context>
@/Users/aviyadava/.codex/get-shit-done/workflows/execute-plan.md
@/Users/aviyadava/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-llm-client-and-tactical-planner/05-CONTEXT.md
@.planning/phases/05-llm-client-and-tactical-planner/05-RESEARCH.md
@.planning/phases/05-llm-client-and-tactical-planner/05-VERIFICATION.md

<focus>
Gap evidence to close:
- Runtime does not currently emit `planner:context-ready` from startup wiring.
- TacticalPlanner falls back to `context: null` when no context bundle was seen.
- Verification requires direct test evidence that non-null context reaches LLM payload before call.
- Human verification (live Fireworks + simple autonomous progression) must be re-run post-fix.
</focus>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add automated non-null context coverage (unit + startup integration)</name>
  <files>src/planner/TacticalPlanner.test.ts, src/index.planner-context.integration.test.ts</files>
  <action>
Add focused automated coverage that proves non-null context reaches TacticalPlanner before LLM calls:
- In `src/planner/TacticalPlanner.test.ts`, emit `planner:context-ready` with a realistic `PlannerContextBundle`, then emit `executor:result`, and assert the LLM stub receives `messages[1]` where serialized user payload contains a non-null `context` object with snapshot + memory fields.
- In `src/index.planner-context.integration.test.ts`, boot `initializeApplication` with deterministic stubs, trigger spawn + perception update, assert runtime emits `planner:context-ready`, and assert TacticalPlanner call path sees non-null context before first tactical cycle.

Use project test style (`assert` + script entrypoint), avoid jest/vitest, and keep both tests deterministic. Coverage should fail against null-context wiring assumptions and pass once runtime context flow is wired.
  </action>
  <verify>
    <automated>npx tsx src/planner/TacticalPlanner.test.ts && npx tsx src/index.planner-context.integration.test.ts</automated>
  </verify>
  <done>Automated unit + startup integration coverage proves runtime context emission and non-null context delivery into TacticalPlanner's LLM payload path.</done>
</task>

<task type="auto">
  <name>Task 2: Wire ContextAssembler into runtime startup flow</name>
  <files>src/index.ts</files>
  <action>
Update startup wiring to assemble planner context from real `perception:updated` events and emit `planner:context-ready` through EventBus:
- Instantiate `ContextAssembler` at runtime startup.
- Create planner-memory retriever using existing memory repositories (`createPlannerMemoryRetriever`).
- Subscribe to `perception:updated` and call `assembleAndPublishPlannerContext` with:
  - latest perception snapshot,
  - intent derived from WorkingMemory (`activePlan?.goal`, `activeSubgoalId`, `execution.inFlightAction?.skill`),
  - retriever bound to snapshot position + active goal.
- Keep async handler `no-floating-promises` compliant and add compact error logging on assemble failures.
- Ensure new listeners are removed in shutdown cleanup.

Do not change TacticalPlanner policy logic from 05-02; only close the missing runtime context-input boundary.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx eslint src --ext .ts</automated>
  </verify>
  <done>Runtime now emits planner:context-ready during normal perception updates, and cleanup unregisters all added listeners.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Post-gap live verification for context-to-tactical flow</name>
  <files>src/index.ts, src/planner/TacticalPlanner.ts, src/planner/TacticalPlanner.test.ts, src/index.planner-context.integration.test.ts</files>
  <action>
After automated pre-checks pass, run blocking human verification:

1. Live Fireworks smoke (`FIREWORKS_API_KEY=... npm start`): confirm at least one tactical cycle with successful LLM call, emitted `planner:context-ready`, and non-WAIT `tactical:queue-ready`.
2. In-game simple-goal progression: run a simple move-to + break-block subgoal and confirm executor advances without manual per-action intervention.
3. Regression sanity: confirm no crash loop and unrecoverable JSON path still falls back to WAIT behavior.
  </action>
  <verify>
    <automated>npx tsx src/planner/TacticalPlanner.test.ts && npx tsx src/index.planner-context.integration.test.ts && npx tsx src/planner/FireworksLLMClient.test.ts && npx tsc --noEmit && npx eslint src --ext .ts</automated>
  </verify>
  <done>Checkpoint passes only when automated pre-checks are green and human verification confirms all three runtime checks with `approved`; otherwise capture exact failure logs and reproduction steps for another gap-closure iteration.</done>
  <resume-signal>Type "approved" if all checks pass, or provide exact failure logs/steps for another gap iteration.</resume-signal>
</task>

</tasks>

<verification>
Automated gate before human checkpoint:
```
npx tsx src/planner/TacticalPlanner.test.ts && \
npx tsx src/index.planner-context.integration.test.ts && \
npx tsx src/planner/FireworksLLMClient.test.ts && \
npx tsc --noEmit && \
npx eslint src --ext .ts
```

Human gate after automated pass:
- Live Fireworks call returns valid JSON queue output (PLAN-01)
- Tactical cycle receives assembled context and executes simple goal progression (PLAN-02)
</verification>

<success_criteria>
- `planner:context-ready` is emitted from runtime startup wiring (`src/index.ts`) via perception updates
- TacticalPlanner receives non-null assembled context before LLM call path
- Automated integration test fails if runtime context wiring is removed
- Live Fireworks + in-game simple-goal checks are re-verified after fix
- Phase 05 verification score can move from 1/3 to goal-complete once checkpoint is approved
</success_criteria>

<output>
After execution, create `.planning/phases/05-llm-client-and-tactical-planner/05-04-SUMMARY.md` with evidence links to automated and human verification artifacts.
</output>
