---
phase: 03-perception-layer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/perception/SnapshotBuilder.ts
  - src/perception/NearbyScanner.ts
  - src/perception/types.ts
  - src/types/index.ts
  - src/config.ts
  - src/perception/SnapshotBuilder.test.ts
autonomous: true
requirements:
  - PERC-01

must_haves:
  truths:
    - "PerceptionSnapshot includes all required fields for self-state, inventory, world context, current action, and recent failures"
    - "Nearby entities and nearby blocks are bounded and deterministically ordered to avoid prompt churn"
    - "Snapshot payload is compact and excludes raw mineflayer object leakage"
  artifacts:
    - path: "src/perception/SnapshotBuilder.ts"
      provides: "Pure snapshot construction from bot/runtime inputs"
      contains: "buildPerceptionSnapshot"
    - path: "src/perception/NearbyScanner.ts"
      provides: "Nearest-first entity/block extraction with limits and stable ordering"
      contains: "scanNearbyBlocks"
    - path: "src/perception/SnapshotBuilder.test.ts"
      provides: "Coverage for required fields, caps, and deterministic ordering"
      contains: "nearbyBlocks"
  key_links:
    - from: "src/perception/SnapshotBuilder.ts"
      to: "src/perception/NearbyScanner.ts"
      via: "delegates bounded nearest scans for world context"
      pattern: "scanNearby"
    - from: "src/config.ts"
      to: "src/perception/NearbyScanner.ts"
      via: "provides scan radius and list-size defaults"
      pattern: "nearbyScanRadius"
---

<objective>
Deliver the snapshot data contract for the perception phase by implementing deterministic, compact `PerceptionSnapshot` construction from live bot state.

Purpose: This is the schema and signal-quality foundation for all later cadence and context-assembly work.
Output: Snapshot builder, nearby scanner, and tests proving PERC-01 contract fidelity.
</objective>

<execution_context>
@/Users/aviyadava/.codex/get-shit-done/workflows/execute-plan.md
@/Users/aviyadava/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/aviyadava/minecraft-bot/.planning/ROADMAP.md
@/Users/aviyadava/minecraft-bot/.planning/REQUIREMENTS.md
@/Users/aviyadava/minecraft-bot/.planning/STATE.md
@/Users/aviyadava/minecraft-bot/.planning/phases/03-perception-layer/03-CONTEXT.md
@/Users/aviyadava/minecraft-bot/.planning/phases/03-perception-layer/03-RESEARCH.md
@/Users/aviyadava/minecraft-bot/.planning/phases/03-perception-layer/03-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement snapshot and nearby scan primitives</name>
  <files>src/perception/SnapshotBuilder.ts, src/perception/NearbyScanner.ts, src/perception/types.ts, src/types/index.ts, src/config.ts</files>
  <action>
    Add perception primitives that build a compact `PerceptionSnapshot` from live runtime state:
    - create a nearby scanner with nearest-first extraction and fixed caps (entities: 12, blocks: 16)
    - enforce deterministic tie-breaking for equally distant candidates
    - map required bot state fields (`position`, `health`, `food`, armor/equipment signal, inventory summary, time-of-day, current action, recent failures)
    - add perception config defaults for scan radius and limits

    Keep snapshot output strictly typed and free of raw mineflayer entity/block payloads.
  </action>
  <verify>
    <automated>cd /Users/aviyadava/minecraft-bot && npm run typecheck</automated>
  </verify>
  <done>Snapshot primitives compile with the full required PERC-01 field surface and deterministic ordering rules</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add field-contract and determinism tests for snapshots</name>
  <files>src/perception/SnapshotBuilder.test.ts</files>
  <action>
    Add tests that validate:
    - every PERC-01 required snapshot field is present
    - nearby entity/block arrays respect configured caps
    - ordering is stable and deterministic for equivalent candidates
    - projection is compact (inventory as counts, no raw object leaks)
    - `recentFailures` remains bounded.
  </action>
  <verify>
    <automated>cd /Users/aviyadava/minecraft-bot && npm run typecheck && npm run lint && npx tsx src/perception/SnapshotBuilder.test.ts</automated>
  </verify>
  <done>Automated checks prove snapshot contract integrity and deterministic compact output behavior</done>
</task>

</tasks>

<verification>
Run required checks:

```bash
cd /Users/aviyadava/minecraft-bot
npm run typecheck
npm run lint
```

Manual spot-check:
1. Build one sample snapshot from fixture state.
2. Confirm all PERC-01 fields exist and nearby lists are bounded/sorted.
3. Confirm payload does not include raw mineflayer object graphs.
</verification>

<success_criteria>
- `PERC-01` snapshot contract is implemented with deterministic and compact projections
- Nearby selection limits and ordering policies are codified and tested
- Snapshot builder is ready for scheduler/event emission integration in the next wave
</success_criteria>

<output>
After completion, create `/Users/aviyadava/minecraft-bot/.planning/phases/03-perception-layer/03-01-SUMMARY.md`.
</output>
