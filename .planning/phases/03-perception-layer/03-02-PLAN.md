---
phase: 03-perception-layer
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - src/perception/PerceptionService.ts
  - src/perception/PerceptionCadence.ts
  - src/events/EventBus.ts
  - src/index.ts
  - src/perception/PerceptionService.test.ts
autonomous: true
requirements:
  - PERC-02

must_haves:
  truths:
    - "Snapshot generation runs on a controlled cadence in the 1-2 Hz band during normal operation"
    - "Event-driven burst updates are bounded by a hard max rate and short cooldown back to baseline"
    - "Each snapshot cycle emits exactly one `perception:updated` event payload on EventBus"
  artifacts:
    - path: "src/perception/PerceptionService.ts"
      provides: "Lifecycle service for scheduler, debounce, burst handling, and EventBus emission"
      contains: "start"
    - path: "src/perception/PerceptionCadence.ts"
      provides: "Pure cadence/debounce/cooldown policy helper logic"
      contains: "nextEmitAt"
    - path: "src/perception/PerceptionService.test.ts"
      provides: "Temporal behavior tests for heartbeat, burst cap, and single-emission cycles"
      contains: "perception:updated"
  key_links:
    - from: "src/index.ts"
      to: "src/perception/PerceptionService.ts"
      via: "starts perception after memory readiness and bot creation"
      pattern: "initializeApplication"
    - from: "src/perception/PerceptionService.ts"
      to: "src/events/EventBus.ts"
      via: "publishes debounced snapshot updates"
      pattern: "eventBus.emit('perception:updated'"
---

<objective>
Implement cadence-controlled perception emission so snapshot publication is stable at baseline frequency, burst-aware under activity, and fully event-bus driven.

Purpose: This enforces PERC-02 behavior guarantees and protects downstream planning loops from tick floods and duplicate updates.
Output: Perception runtime service with cadence policy, lifecycle wiring, and temporal integration tests.
</objective>

<execution_context>
@/Users/aviyadava/.codex/get-shit-done/workflows/execute-plan.md
@/Users/aviyadava/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/aviyadava/minecraft-bot/.planning/ROADMAP.md
@/Users/aviyadava/minecraft-bot/.planning/REQUIREMENTS.md
@/Users/aviyadava/minecraft-bot/.planning/STATE.md
@/Users/aviyadava/minecraft-bot/.planning/phases/03-perception-layer/03-CONTEXT.md
@/Users/aviyadava/minecraft-bot/.planning/phases/03-perception-layer/03-RESEARCH.md
@/Users/aviyadava/minecraft-bot/.planning/phases/03-perception-layer/03-VALIDATION.md
@/Users/aviyadava/minecraft-bot/.planning/phases/03-perception-layer/03-01-PLAN.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build perception cadence scheduler and event-driven burst policy</name>
  <files>src/perception/PerceptionCadence.ts, src/perception/PerceptionService.ts</files>
  <action>
    Implement cadence policy and runtime service behavior:
    - baseline emission in adaptive 1-2 Hz range with idle heartbeat
    - event-driven acceleration for relevant state changes
    - hard burst max-rate cap and cooldown to return to baseline
    - near-duplicate suppression so equivalent snapshots do not emit repeatedly
    - explicit `start/stop/getLastSnapshot` lifecycle API.

    Keep cadence logic isolated in a testable policy helper module.
  </action>
  <verify>
    <automated>cd /Users/aviyadava/minecraft-bot && npm run typecheck</automated>
  </verify>
  <done>Perception cadence policy compiles with deterministic scheduler state transitions and burst control</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire EventBus emission and startup integration</name>
  <files>src/events/EventBus.ts, src/index.ts, src/perception/PerceptionService.ts</files>
  <action>
    Integrate perception runtime into app bootstrap:
    - initialize and start perception service after memory startup succeeds
    - subscribe to relevant bot/event-bus signals to mark state dirty
    - emit one `perception:updated` event per completed snapshot cycle
    - ensure shutdown path unsubscribes/listeners cleanly.
  </action>
  <verify>
    <automated>cd /Users/aviyadava/minecraft-bot && npm run typecheck && npm run lint</automated>
  </verify>
  <done>Application wiring publishes debounced perception snapshots via EventBus with clean lifecycle boundaries</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add temporal integration tests for cadence and emission guarantees</name>
  <files>src/perception/PerceptionService.test.ts</files>
  <action>
    Add tests that simulate idle and burst scenarios:
    - idle snapshots remain in 1-2 Hz heartbeat band
    - burst mode cannot exceed configured max rate
    - cooldown returns cadence to baseline
    - exactly one `perception:updated` event is emitted for each produced snapshot cycle.
  </action>
  <verify>
    <automated>cd /Users/aviyadava/minecraft-bot && npm run typecheck && npm run lint && npm run build && npx tsx src/perception/PerceptionService.test.ts</automated>
  </verify>
  <done>Temporal tests demonstrate PERC-02 cadence/debounce/event emission behavior under representative load</done>
</task>

</tasks>

<verification>
Run cadence checks:

```bash
cd /Users/aviyadava/minecraft-bot
npm run typecheck
npm run lint
npm run build
```

Manual runtime check:
1. Run bot on local server.
2. Observe idle cadence remains ~1-2 Hz.
3. Trigger activity (movement/chat/executor results) and confirm bounded burst behavior with cooldown.
</verification>

<success_criteria>
- `PERC-02` debounced/event-aware snapshot generation is implemented and emitted on EventBus
- Perception service starts/stops cleanly from application lifecycle
- Temporal integration tests cover heartbeat, burst cap, and cooldown transitions
</success_criteria>

<output>
After completion, create `/Users/aviyadava/minecraft-bot/.planning/phases/03-perception-layer/03-02-SUMMARY.md`.
</output>
