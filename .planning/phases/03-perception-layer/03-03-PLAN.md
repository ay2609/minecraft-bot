---
phase: 03-perception-layer
plan: 03
type: execute
wave: 3
depends_on:
  - 03-01
  - 03-02
files_modified:
  - src/perception/ContextAssembler.ts
  - src/perception/types.ts
  - src/memory/index.ts
  - src/events/EventBus.ts
  - src/perception/ContextAssembler.test.ts
  - src/perception/PerceptionContext.integration.test.ts
autonomous: false
requirements:
  - PERC-03

must_haves:
  truths:
    - "Planner-facing context is composed from snapshot + working memory intent + targeted semantic/episodic memory"
    - "Assembler output is compact, structured, and budgeted with deterministic drop policy under pressure"
    - "Downstream consumers use assembled context object, never raw mineflayer state or raw DB row dumps"
    - "Perception snapshot cadence remains independent from memory/query workload via non-blocking assembly path"
  artifacts:
    - path: "src/perception/ContextAssembler.ts"
      provides: "Prompt-ready context composition with relevance filtering and budget enforcement"
      contains: "assemblePlannerContext"
    - path: "src/perception/types.ts"
      provides: "Planner context contracts and budget metadata types"
      contains: "PlannerContextBundle"
    - path: "src/perception/ContextAssembler.test.ts"
      provides: "Coverage for compactness, relevance filtering, and drop-order behavior"
      contains: "older episodic"
  key_links:
    - from: "src/perception/ContextAssembler.ts"
      to: "src/memory/index.ts"
      via: "queries semantic/episodic slices relevant to goal and location"
      pattern: "episodic"
    - from: "src/perception/ContextAssembler.ts"
      to: "src/events/EventBus.ts"
      via: "publishes mandatory assembled planner-context boundary event"
      pattern: "planner:context-ready"
---

<objective>
Complete Phase 03 by implementing context assembly that converts perception and memory into a compact planner-ready bundle.

Purpose: This closes the phase by delivering the exact object boundary consumed by future planning loops while enforcing strict data-budget and relevance constraints.
Output: Context assembler module, contracts, and integration tests proving PERC-03 behavior.
</objective>

<execution_context>
@/Users/aviyadava/.codex/get-shit-done/workflows/execute-plan.md
@/Users/aviyadava/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/aviyadava/minecraft-bot/.planning/ROADMAP.md
@/Users/aviyadava/minecraft-bot/.planning/REQUIREMENTS.md
@/Users/aviyadava/minecraft-bot/.planning/STATE.md
@/Users/aviyadava/minecraft-bot/.planning/phases/03-perception-layer/03-CONTEXT.md
@/Users/aviyadava/minecraft-bot/.planning/phases/03-perception-layer/03-RESEARCH.md
@/Users/aviyadava/minecraft-bot/.planning/phases/03-perception-layer/03-VALIDATION.md
@/Users/aviyadava/minecraft-bot/.planning/phases/03-perception-layer/03-01-PLAN.md
@/Users/aviyadava/minecraft-bot/.planning/phases/03-perception-layer/03-02-PLAN.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build budgeted context assembler with non-blocking memory attachment</name>
  <files>src/perception/types.ts, src/perception/ContextAssembler.ts, src/perception/ContextAssembler.test.ts</files>
  <action>
    Define `PlannerContextBundle` contracts and implement a typed assembler that:
    - consumes current snapshot + working-memory intent + relevant semantic/episodic retrieval
    - summarizes memory to compact structured slices (no raw row dumps)
    - enforces budget rules with deterministic trimming (drop older episodic detail first)
    - emits metadata about truncation/degradation decisions for observability
    - uses timeout/cached-fallback memory attachment so context assembly cannot block perception cadence.
  </action>
  <verify>
    <automated>cd /Users/aviyadava/minecraft-bot && npm run typecheck && npm run lint && npx tsx src/perception/ContextAssembler.test.ts</automated>
  </verify>
  <done>Assembler contracts enforce compactness/drop-order and include explicit non-blocking memory fallback behavior</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce mandatory planner-context boundary and verify cadence independence</name>
  <files>src/memory/index.ts, src/events/EventBus.ts, src/perception/ContextAssembler.ts</files>
  <action>
    Wire assembler integration for downstream use:
    - expose a stable API for obtaining assembled planner context from latest perception snapshot
    - pull targeted memory slices by proximity, recency, and active goal relevance
    - add a mandatory typed `planner:context-ready` boundary consumed by downstream planning code
    - prevent any direct raw-state/raw-repository payload propagation across layer boundaries
    - keep snapshot emission cadence independent of slow memory queries by decoupling query execution from cadence loop.
  </action>
  <verify>
    <automated>cd /Users/aviyadava/minecraft-bot && npm run typecheck && npm run lint && npx tsx src/perception/PerceptionContext.integration.test.ts</automated>
  </verify>
  <done>Planner consumers are forced through `planner:context-ready`, with tests proving no cadence slowdown under memory/query pressure</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Validate planner context quality on real runtime traces</name>
  <files>N/A</files>
  <action>
Capture representative perception snapshots from idle and active gameplay, run them through assembler, and inspect outputs for:
    1. signal density (relevant, non-bloated context)
    2. no raw mineflayer/DB dump leakage
    3. consistent structure suitable for strict JSON planner prompts
    4. cadence remains in expected baseline range while memory backends are intentionally slowed.
  </action>
  <done>Human review confirms planner-ready context quality and compactness expectations from Phase 03 goal</done>
</task>

</tasks>

<verification>
Run full phase gate checks:

```bash
cd /Users/aviyadava/minecraft-bot
npm run typecheck
npm run lint
npm run build
npx tsx src/perception/ContextAssembler.test.ts
npx tsx src/perception/PerceptionContext.integration.test.ts
```

Capture and review at least three assembled context samples (idle, movement-heavy, failure-recovery) and one slowed-memory run proving cadence remains stable; record evidence in summary.
</verification>

<success_criteria>
- `PERC-03` compact context assembly is implemented and integrated behind a typed boundary
- Context includes snapshot + intent + relevant memory and respects deterministic budget controls
- Test and human validation confirm no raw-state/raw-row leakage to downstream model consumers
</success_criteria>

<output>
After completion, create `/Users/aviyadava/minecraft-bot/.planning/phases/03-perception-layer/03-03-SUMMARY.md`.
</output>
