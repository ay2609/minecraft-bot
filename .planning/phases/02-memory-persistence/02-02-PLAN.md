---
phase: 02-memory-persistence
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - src/memory/WorkingMemory.ts
  - src/memory/CheckpointRepository.ts
  - src/memory/WorkingMemoryRestore.ts
  - src/types/index.ts
  - src/events/EventBus.ts
  - src/memory/WorkingMemory.test.ts
autonomous: true
requirements:
  - MEM-01
  - MEM-04

must_haves:
  truths:
    - "Working memory is the single in-process source of truth for current plan, active subgoal, action queue, and constraints"
    - "A committed plan checkpoint can be loaded on startup to reconstruct working memory intent"
    - "Restore policy clears unsafe in-flight execution state instead of auto-continuing stale actions"
  artifacts:
    - path: "src/memory/WorkingMemory.ts"
      provides: "Typed singleton working-memory service with controlled mutators"
      contains: "setPlan"
    - path: "src/memory/WorkingMemoryRestore.ts"
      provides: "Startup reconstruction workflow with strict validation + safety clear"
      contains: "restoreFromCheckpoint"
    - path: "src/events/EventBus.ts"
      provides: "Memory lifecycle events for readiness and restore status"
      contains: "memory:restore-complete"
  key_links:
    - from: "src/memory/WorkingMemory.ts"
      to: "src/memory/CheckpointRepository.ts"
      via: "persists durable plan-state boundaries"
      pattern: "commitCheckpoint"
    - from: "src/memory/WorkingMemoryRestore.ts"
      to: "src/events/EventBus.ts"
      via: "emits restore success/failure lifecycle events"
      pattern: "memory:restore"
---

<objective>
Implement the in-process working memory model and safe restart reconstruction policy from committed plan state.

Purpose: This plan delivers the behavioral core of memory continuity. It must preserve goal context across restart while intentionally avoiding unsafe continuation of stale in-flight executor state.
Output: Working memory service, checkpoint persistence model, and restore workflow/events.
</objective>

<execution_context>
@/Users/aviyadava/.codex/get-shit-done/workflows/execute-plan.md
@/Users/aviyadava/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/aviyadava/minecraft-bot/.planning/ROADMAP.md
@/Users/aviyadava/minecraft-bot/.planning/REQUIREMENTS.md
@/Users/aviyadava/minecraft-bot/.planning/phases/02-memory-persistence/02-CONTEXT.md
@/Users/aviyadava/minecraft-bot/.planning/phases/02-memory-persistence/02-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build working-memory service with strict mutation API</name>
  <files>src/memory/WorkingMemory.ts, src/types/index.ts, src/memory/WorkingMemory.test.ts</files>
  <action>
    Implement `WorkingMemory` as a single owned in-process state container with explicit methods:
    - `getSnapshot`
    - `setPlan`
    - `setActiveSubgoal`
    - `setActionQueue`
    - `setConstraints`
    - `clearExecutionTransients`
    - `reset`

    Add/extend shared types for working-memory snapshot and restore metadata so state is compile-verified.
    Ensure external code cannot mutate internal state object directly.
  </action>
  <verify>
    <automated>cd /Users/aviyadava/minecraft-bot && npm run typecheck</automated>
  </verify>
  <done>Working memory APIs compile and enforce single-source-of-truth semantics for current bot intent</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add checkpoint persistence and restore workflow</name>
  <files>src/memory/CheckpointRepository.ts, src/memory/WorkingMemoryRestore.ts, src/events/EventBus.ts</files>
  <action>
    Implement checkpoint persistence for durable plan state boundaries and startup reconstruction:
    - Add `CheckpointRepository` (using database from 02-01) to write/load latest active checkpoint.
    - Implement `WorkingMemoryRestore` that:
      - validates checkpoint payloads before loading
      - reconstructs plan/subgoal/queue/constraints into working memory
      - clears unsafe transients (in-flight action execution details)
      - emits `memory:restore-complete` on success and `memory:restore-failed` on corruption/incompatibility.

    Extend EventBus typings for memory lifecycle events: `memory:ready`, `memory:restore-complete`, `memory:restore-failed`, `memory:persistence-error`.
  </action>
  <verify>
    <automated>cd /Users/aviyadava/minecraft-bot && npm run typecheck && npm run lint</automated>
  </verify>
  <done>Checkpoint-based restart reconstruction works by design and matches Phase 2 restart policy</done>
</task>

</tasks>

<verification>
Verify working-memory restore behavior:

```bash
cd /Users/aviyadava/minecraft-bot
npm run typecheck
npm run lint
```

Manual/targeted check:
1. Create a checkpoint for a non-empty plan/subgoal/queue.
2. Restart restore workflow and confirm snapshot returns same goal intent.
3. Confirm transient executor fields are cleared and not resumed automatically.
4. Confirm restore-failure path emits typed failure event and does not silently proceed.
</verification>

<success_criteria>
- `MEM-01` implemented with controlled in-process state ownership
- `MEM-04` reconstruction policy implemented from committed checkpoint state
- EventBus publishes memory lifecycle events used by future phases
</success_criteria>

<output>
After completion, create `/Users/aviyadava/minecraft-bot/.planning/phases/02-memory-persistence/02-02-SUMMARY.md`.
</output>
