---
phase: 02-memory-persistence
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/memory/Database.ts
  - src/memory/schema.ts
  - src/memory/Database.test.ts
  - src/config.ts
autonomous: true
requirements:
  - FOUND-04

must_haves:
  truths:
    - "SQLite opens during startup in WAL mode and reports journal_mode=wal"
    - "All required tables exist after initialization: locations, resources, routes, structures, server_facts, episodes"
    - "Schema initialization is idempotent and safe to run on every startup"
  artifacts:
    - path: "src/memory/Database.ts"
      provides: "SQLite lifecycle manager with startup pragmas and schema bootstrap"
      contains: "journal_mode = WAL"
    - path: "src/memory/schema.ts"
      provides: "Canonical DDL statements for required memory tables and indexes"
      contains: "CREATE TABLE IF NOT EXISTS episodes"
    - path: "src/memory/Database.test.ts"
      provides: "Automated assertions for pragmas + required schema presence"
      contains: "SELECT name FROM sqlite_master"
  key_links:
    - from: "src/memory/Database.ts"
      to: "src/memory/schema.ts"
      via: "applies schema statements during init"
      pattern: "initializeSchema"
    - from: "src/config.ts"
      to: "src/memory/Database.ts"
      via: "provides dbPath and sqlite startup settings"
      pattern: "dbPath"
---

<objective>
Build the persistence foundation for Phase 2 by introducing deterministic SQLite bootstrap with WAL mode and required semantic/episodic tables.

Purpose: This is the hard gate for every remaining memory task. If startup pragmas and schema creation are not deterministic, restart durability and memory queries will be unstable.
Output: A memory database module that can initialize schema on startup and prove readiness via tests.
</objective>

<execution_context>
@/Users/aviyadava/.codex/get-shit-done/workflows/execute-plan.md
@/Users/aviyadava/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/aviyadava/minecraft-bot/.planning/ROADMAP.md
@/Users/aviyadava/minecraft-bot/.planning/REQUIREMENTS.md
@/Users/aviyadava/minecraft-bot/.planning/STATE.md
@/Users/aviyadava/minecraft-bot/.planning/phases/02-memory-persistence/02-CONTEXT.md
@/Users/aviyadava/minecraft-bot/.planning/phases/02-memory-persistence/02-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create database bootstrap module and canonical schema</name>
  <files>src/memory/Database.ts, src/memory/schema.ts, src/config.ts</files>
  <action>
    Add a new `src/memory/` directory and implement:
    - `schema.ts` exporting deterministic SQL statements for required tables:
      `locations`, `resources`, `routes`, `structures`, `server_facts`, `episodes`.
    - `Database.ts` that:
      - creates/opens SQLite connection via `better-sqlite3`
      - applies startup pragmas (`journal_mode=WAL`, `synchronous=NORMAL`, `foreign_keys=ON`, `busy_timeout`)
      - runs schema statements in a transaction
      - exposes methods to query readiness and close the DB.
    - Extend `src/config.ts` with memory config:
      - `memory.dbPath` (default `./data/memory.db`)
      - optional sqlite timeout config.

    Keep interfaces small and typed; no planner/executor logic in this plan.
  </action>
  <verify>
    <automated>cd /Users/aviyadava/minecraft-bot && npm run typecheck</automated>
  </verify>
  <done>Database bootstrap compiles and required schema statements are centralized in `src/memory/schema.ts`</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add schema + WAL initialization tests</name>
  <files>src/memory/Database.test.ts</files>
  <action>
    Add a TypeScript test file that boots the database against a temp path and validates:
    - `PRAGMA journal_mode` returns `wal`
    - all required tables exist in `sqlite_master`
    - re-running initialization does not error (idempotency).

    Follow existing repository style (compile-checked tests if no runtime test harness exists).
  </action>
  <verify>
    <automated>cd /Users/aviyadava/minecraft-bot && npm run typecheck && npm run lint</automated>
  </verify>
  <done>Automated checks exist proving `FOUND-04` startup behavior and schema guarantees</done>
</task>

</tasks>

<verification>
Run startup-level validation commands:

```bash
cd /Users/aviyadava/minecraft-bot
npm run typecheck
npm run lint
```

Then run a short script/manual query against a temp DB path to confirm:
- `PRAGMA journal_mode;` outputs `wal`
- `SELECT name FROM sqlite_master WHERE type='table';` includes all six required tables.
</verification>

<success_criteria>
- Database module initializes SQLite with WAL mode at startup
- Required tables exist after init and schema is idempotent
- `FOUND-04` appears explicitly in requirement coverage
</success_criteria>

<output>
After completion, create `/Users/aviyadava/minecraft-bot/.planning/phases/02-memory-persistence/02-01-SUMMARY.md`.
</output>
