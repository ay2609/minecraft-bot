---
phase: 02-memory-persistence
plan: 03
type: execute
wave: 3
depends_on:
  - 02-01
  - 02-02
files_modified:
  - src/memory/SemanticMemoryRepository.ts
  - src/memory/EpisodicMemoryRepository.ts
  - src/memory/index.ts
  - src/index.ts
  - src/memory/SemanticMemoryRepository.test.ts
  - src/memory/EpisodicMemoryRepository.test.ts
  - src/memory/MemoryRestart.integration.test.ts
requirements:
  - MEM-02
  - MEM-03
  - MEM-04
autonomous: false

must_haves:
  truths:
    - "Semantic memory persists locations/resources/routes/structures/server facts and supports typed query patterns (including proximity)"
    - "Episodic memory persists attempts/outcomes/failure reasons/success context with timestamps and goal context"
    - "After restart, semantic + episodic entries are immediately queryable and working memory restore is invoked before higher loops begin"
  artifacts:
    - path: "src/memory/SemanticMemoryRepository.ts"
      provides: "Durable semantic-memory write/query APIs over required domain tables"
      contains: "findNearby"
    - path: "src/memory/EpisodicMemoryRepository.ts"
      provides: "Durable episodic append/query APIs with goal-context filters"
      contains: "recordEpisode"
    - path: "src/index.ts"
      provides: "Startup integration that initializes memory stack before runtime loops"
      contains: "memory:ready"
  key_links:
    - from: "src/index.ts"
      to: "src/memory/index.ts"
      via: "initializes DB + repositories + restore before continuing boot"
      pattern: "initializeMemory"
    - from: "src/memory/MemoryRestart.integration.test.ts"
      to: "MEM-04"
      via: "writes data, restarts process context, verifies persistence + reconstruction"
      pattern: "restart"
---

<objective>
Complete Phase 2 by implementing semantic and episodic repositories and integrating startup initialization/restoration end-to-end.

Purpose: This plan converts the phase architecture into observable restart durability behavior: durable memory remains available across process restarts, and working memory is reconstructed from committed plan intent before runtime loops begin.
Output: Repository APIs, startup wiring, and restart durability verification for semantic/episodic plus working-memory reconstruction.
</objective>

<execution_context>
@/Users/aviyadava/.codex/get-shit-done/workflows/execute-plan.md
@/Users/aviyadava/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/aviyadava/minecraft-bot/.planning/ROADMAP.md
@/Users/aviyadava/minecraft-bot/.planning/REQUIREMENTS.md
@/Users/aviyadava/minecraft-bot/.planning/STATE.md
@/Users/aviyadava/minecraft-bot/.planning/phases/02-memory-persistence/02-CONTEXT.md
@/Users/aviyadava/minecraft-bot/.planning/phases/02-memory-persistence/02-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement semantic and episodic repository APIs</name>
  <files>src/memory/SemanticMemoryRepository.ts, src/memory/EpisodicMemoryRepository.ts, src/memory/index.ts</files>
  <action>
    Build typed repository modules on top of the database bootstrap from 02-01:
    - `SemanticMemoryRepository` for `locations`, `resources`, `routes`, `structures`, `server_facts`.
      Include query methods by type and proximity (`dimension + coordinate window + distance sort`).
    - `EpisodicMemoryRepository` for `episodes`.
      Include append method and retrieval methods by goal type, outcome/failure, recency.
    - `src/memory/index.ts` should expose a clean memory API surface used by startup.

    Keep SQL operations parameterized and aligned to schema/indexes introduced in 02-01.
  </action>
  <verify>
    <automated>cd /Users/aviyadava/minecraft-bot && npm run typecheck</automated>
  </verify>
  <done>Durable semantic/episodic memory APIs compile and map directly to required tables</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Integrate startup ordering and restart durability tests</name>
  <files>src/index.ts, src/memory/SemanticMemoryRepository.test.ts, src/memory/EpisodicMemoryRepository.test.ts, src/memory/MemoryRestart.integration.test.ts</files>
  <action>
    Update startup flow to initialize memory before long-running runtime loops:
    1. Load config
    2. Initialize database + schema
    3. Instantiate repositories + working memory
    4. Run checkpoint-based restore
    5. Emit readiness events
    6. Continue bot startup only after memory initialization success.

    Add tests that verify:
    - semantic writes survive restart and remain queryable
    - episodic writes survive restart and remain queryable
    - working memory restore is invoked from committed checkpoint
    - restore failure path blocks unsafe startup continuation.
  </action>
  <verify>
    <automated>cd /Users/aviyadava/minecraft-bot && npm run typecheck && npm run lint</automated>
  </verify>
  <done>Phase 2 restart durability behavior is implemented and checked against `MEM-02`, `MEM-03`, and `MEM-04`</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Confirm memory survives restart end-to-end</name>
  <files>N/A</files>
  <action>
    Verify on a local DB path:
    1. Start app and persist one semantic fact and one episode.
    2. Persist a checkpoint with active plan context.
    3. Stop process and restart.
    4. Confirm semantic + episodic entries are available immediately.
    5. Confirm working memory reflects restored goal/subgoal intent but does not auto-execute stale in-flight action.
  </action>
  <done>Human verification confirms all Phase 2 success criteria from roadmap are observable after restart</done>
</task>

</tasks>

<verification>
Required validation before marking phase complete:

```bash
cd /Users/aviyadava/minecraft-bot
npm run typecheck
npm run lint
```

Run restart durability test path (script/test/manual) and record evidence in summary:
- table/query outputs for semantic + episodic persistence
- restore event logs for success/failure scenarios
- checkpoint restoration evidence for prior goal continuity.
</verification>

<success_criteria>
- `MEM-02` semantic persistence implemented with required domain coverage
- `MEM-03` episodic persistence implemented with queryable outcome history
- `MEM-04` restart behavior verified end-to-end with safe reconstruction policy
</success_criteria>

<output>
After completion, create `/Users/aviyadava/minecraft-bot/.planning/phases/02-memory-persistence/02-03-SUMMARY.md`.
</output>
