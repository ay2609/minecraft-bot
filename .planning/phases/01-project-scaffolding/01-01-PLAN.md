---
phase: 01-project-scaffolding
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - .eslintrc.json
  - .gitignore
  - .env.example
autonomous: true
requirements:
  - FOUND-01

must_haves:
  truths:
    - "npm install completes without errors (zero unresolved peer deps)"
    - "tsc --noEmit exits 0 against the src/ directory with strict mode enabled"
    - "ESLint reports no-floating-promises violations when an unawaited async call is present in a test file"
  artifacts:
    - path: "package.json"
      provides: "Project manifest with all runtime and dev dependencies declared"
      contains: '"module": "commonjs"'
    - path: "tsconfig.json"
      provides: "TypeScript compiler configuration — CJS, ES2022, strict"
      contains: '"module": "CommonJS"'
    - path: ".eslintrc.json"
      provides: "ESLint config enforcing no-floating-promises"
      contains: '"no-floating-promises"'
  key_links:
    - from: "tsconfig.json"
      to: "mineflayer CJS ecosystem"
      via: '"module": "CommonJS"'
      pattern: '"module":\s*"CommonJS"'
    - from: ".eslintrc.json"
      to: "@typescript-eslint/eslint-plugin"
      via: "plugin:@typescript-eslint/recommended-type-checked"
      pattern: "no-floating-promises"
---

<objective>
Initialize the Node.js TypeScript project with all dependencies, compiler configuration, and ESLint enforcement that every subsequent phase will build on.

Purpose: Every phase depends on this scaffold being correct. Getting CJS module mode wrong now breaks mineflayer's entire plugin ecosystem. Getting no-floating-promises wrong now allows async event handler bugs to accumulate silently across all phases.
Output: A compilable, lint-checked TypeScript project ready for source files.
</objective>

<execution_context>
@/Users/aviyadava/.claude/get-shit-done/workflows/execute-plan.md
@/Users/aviyadava/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/aviyadava/minecraft-bot/.planning/PROJECT.md
@/Users/aviyadava/minecraft-bot/.planning/ROADMAP.md
@/Users/aviyadava/minecraft-bot/.planning/STATE.md
@/Users/aviyadava/minecraft-bot/.planning/research/STACK.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create package.json with full dependency manifest</name>
  <files>package.json</files>
  <action>
    Create package.json at /Users/aviyadava/minecraft-bot/package.json with:

    - `"name": "minecraft-bot"`
    - `"version": "0.1.0"`
    - `"private": true`
    - `"main": "dist/index.js"`
    - `"scripts"`:
      - `"start": "tsx src/index.ts"`
      - `"build": "tsc"`
      - `"lint": "eslint src --ext .ts"`
      - `"typecheck": "tsc --noEmit"`
    - `"dependencies"`:
      - `"mineflayer": "^4.35.0"`
      - `"mineflayer-pathfinder": "^2.4.5"`
      - `"mineflayer-collectblock": "^1.6.0"`
      - `"mineflayer-auto-eat": "^5.0.3"`
      - `"openai": "^6.27.0"`
      - `"better-sqlite3": "^12.6.2"`
      - `"zod": "^4.3.6"`
    - `"devDependencies"`:
      - `"typescript": "^5.9.3"`
      - `"tsx": "^4.21.0"`
      - `"@types/node": "^20.0.0"`
      - `"@types/better-sqlite3": "^7.6.13"`
      - `"eslint": "^8.57.0"`
      - `"@typescript-eslint/eslint-plugin": "^7.18.0"`
      - `"@typescript-eslint/parser": "^7.18.0"`

    DO NOT include ink/react in this plan — that dependency is only needed in Phase 8 and has an unresolved React 19 + mineflayer peer dep concern (noted in STATE.md blockers). Do not add it now.

    DO NOT include `"type": "module"` — that would break mineflayer. CJS is required.

    Then run: `npm install` at /Users/aviyadava/minecraft-bot/

    If node_modules already exist, run `npm install` anyway to ensure lockfile is consistent.
  </action>
  <verify>
    <automated>cd /Users/aviyadava/minecraft-bot && node -e "require('mineflayer')" && echo "mineflayer require OK"</automated>
  </verify>
  <done>package.json exists with all declared dependencies, npm install exits 0, mineflayer is require()-able from Node.js</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create tsconfig.json and .eslintrc.json</name>
  <files>tsconfig.json, .eslintrc.json, .gitignore, .env.example</files>
  <action>
    Create /Users/aviyadava/minecraft-bot/tsconfig.json:

    ```json
    {
      "compilerOptions": {
        "target": "ES2022",
        "module": "CommonJS",
        "moduleResolution": "node",
        "lib": ["ES2022"],
        "outDir": "./dist",
        "rootDir": "./src",
        "strict": true,
        "esModuleInterop": true,
        "skipLibCheck": true,
        "resolveJsonModule": true,
        "declaration": true,
        "sourceMap": true
      },
      "include": ["src/**/*"],
      "exclude": ["node_modules", "dist"]
    }
    ```

    `module: "CommonJS"` is MANDATORY — mineflayer and all its plugins are CJS. Do NOT change to ESNext or NodeNext. `skipLibCheck: true` is required for mineflayer-pathfinder which ships a d.ts not declared in its package.json.

    Create /Users/aviyadava/minecraft-bot/.eslintrc.json:

    ```json
    {
      "root": true,
      "parser": "@typescript-eslint/parser",
      "parserOptions": {
        "project": "./tsconfig.json",
        "tsconfigRootDir": "."
      },
      "plugins": ["@typescript-eslint"],
      "extends": [
        "eslint:recommended",
        "plugin:@typescript-eslint/recommended-type-checked"
      ],
      "rules": {
        "@typescript-eslint/no-floating-promises": "error",
        "@typescript-eslint/no-explicit-any": "warn",
        "@typescript-eslint/explicit-function-return-type": "off"
      },
      "env": {
        "node": true,
        "es2022": true
      }
    }
    ```

    `no-floating-promises: "error"` (not "warn") is critical — this must be a hard failure from day one per FOUND-01. This requires `parserOptions.project` pointing to tsconfig.json so the plugin can do type-aware analysis.

    Create /Users/aviyadava/minecraft-bot/.gitignore:
    ```
    node_modules/
    dist/
    *.db
    *.db-wal
    *.db-shm
    .env
    ```

    Create /Users/aviyadava/minecraft-bot/.env.example:
    ```
    MINECRAFT_HOST=localhost
    MINECRAFT_PORT=25565
    FIREWORKS_API_KEY=your_key_here
    BOT_USERNAME=ClaudeBot
    ```

    Then create a minimal src/ directory with a stub src/index.ts so that tsc --noEmit has something to type-check:
    ```typescript
    // src/index.ts — stub, replaced in Plan 03
    export {};
    ```

    Run typecheck: `cd /Users/aviyadava/minecraft-bot && npm run typecheck`
  </action>
  <verify>
    <automated>cd /Users/aviyadava/minecraft-bot && npm run typecheck 2>&1 | tail -5</automated>
  </verify>
  <done>tsc --noEmit exits 0, tsconfig.json has "module": "CommonJS", .eslintrc.json has "@typescript-eslint/no-floating-promises": "error"</done>
</task>

</tasks>

<verification>
After both tasks complete, verify ESLint's no-floating-promises is actually enforced (not just configured):

```bash
# Write a file with a floating promise, confirm ESLint reports an error
cd /Users/aviyadava/minecraft-bot
cat > /tmp/eslint_test.ts << 'EOF'
async function doSomething(): Promise<void> {}
doSomething(); // floating promise — ESLint must catch this
EOF
npx eslint --parser-options=project:./tsconfig.json /tmp/eslint_test.ts 2>&1 | grep "no-floating-promises" && echo "ESLint enforcement confirmed" || echo "WARNING: no-floating-promises not firing"
```
</verification>

<success_criteria>
- npm install exits 0 from /Users/aviyadava/minecraft-bot/
- tsc --noEmit exits 0 with strict: true
- .eslintrc.json contains "@typescript-eslint/no-floating-promises": "error"
- tsconfig.json uses "module": "CommonJS" (mineflayer CJS compatibility)
- .env.example documents required environment variables
</success_criteria>

<output>
After completion, create /Users/aviyadava/minecraft-bot/.planning/phases/01-project-scaffolding/01-01-SUMMARY.md
</output>
