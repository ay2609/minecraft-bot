---
phase: 04-skills-and-executor
plan: 03
type: execute
wave: 3
depends_on:
  - 04-01
  - 04-02
files_modified:
  - src/executor/skills/placeBlock.ts
  - src/executor/skills/breakBlock.ts
  - src/executor/skills/interactBlock.ts
  - src/executor/skills/attackEntity.ts
  - src/executor/skills/sendChat.ts
  - src/executor/SkillRegistry.ts
  - src/executor/failureMapping.ts
  - src/executor/UnsafePolicy.smoke.test.ts
  - src/executor/SkillRuntime.smoke.test.ts
autonomous: false
requirements:
  - EXEC-01
  - EXEC-02
  - EXEC-03

must_haves:
  truths:
    - "Unsafe handling is derived from attempted execution outcomes and post-condition checks, not broad global pre-blocking of risky skills"
    - "High-risk world/entity/chat skills return structured ExecutorResult outcomes for both success and failure paths with no uncaught throw"
    - "Failure mapping for attempted unsafe outcomes is deterministic and planner-usable across repeated runs"
    - "Movement mutex behavior remains intact while these non-movement skills execute"
  artifacts:
    - path: "src/executor/skills/placeBlock.ts"
      provides: "Attempt-then-evaluate place flow with structured post-condition failure mapping"
      contains: "executePlaceBlock"
    - path: "src/executor/skills/attackEntity.ts"
      provides: "Attempted combat interaction with deterministic unsafe/blocked outcome mapping"
      contains: "executeAttackEntity"
    - path: "src/executor/failureMapping.ts"
      provides: "Explicit mapping table for attempted execution outcomes into allowed ExecutorErrorCode values"
      contains: "mapExecutorFailure"
    - path: "src/executor/UnsafePolicy.smoke.test.ts"
      provides: "Fast proof that unsafe is inferred from attempted outcomes rather than broad pre-blocking"
      contains: "unsafe policy"
  key_links:
    - from: "src/executor/SkillRegistry.ts"
      to: "src/executor/skills"
      via: "registers concrete implementations for high-risk skill set"
      pattern: "place_block"
    - from: "src/executor/Executor.ts"
      to: "src/executor/failureMapping.ts"
      via: "normalizes attempted execution failures into stable error codes"
      pattern: "mapExecutorFailure"
---

<objective>
Implement high-risk non-movement skills and codify unsafe-policy semantics so unsafe outcomes are determined from attempted execution behavior, not blanket pre-blocking.

Purpose: This closes the checker blocker on unsafe-policy semantics while advancing EXEC-01/EXEC-02 coverage without destabilizing EXEC-03 movement guarantees.
Output: High-risk skill handlers, explicit unsafe mapping rules, and fast smoke coverage.
</objective>

<execution_context>
@/Users/aviyadava/.codex/get-shit-done/workflows/execute-plan.md
@/Users/aviyadava/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/aviyadava/minecraft-bot/.planning/ROADMAP.md
@/Users/aviyadava/minecraft-bot/.planning/REQUIREMENTS.md
@/Users/aviyadava/minecraft-bot/.planning/STATE.md
@/Users/aviyadava/minecraft-bot/.planning/phases/04-skills-and-executor/04-CONTEXT.md
@/Users/aviyadava/minecraft-bot/.planning/phases/04-skills-and-executor/04-RESEARCH.md
@/Users/aviyadava/minecraft-bot/.planning/phases/04-skills-and-executor/04-VALIDATION.md
@/Users/aviyadava/minecraft-bot/.planning/phases/04-skills-and-executor/04-01-PLAN.md
@/Users/aviyadava/minecraft-bot/.planning/phases/04-skills-and-executor/04-02-PLAN.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement high-risk non-movement skill handlers with attempt-derived unsafe semantics</name>
  <files>src/executor/skills/placeBlock.ts, src/executor/skills/breakBlock.ts, src/executor/skills/interactBlock.ts, src/executor/skills/attackEntity.ts, src/executor/skills/sendChat.ts, src/executor/SkillRegistry.ts</files>
  <action>
    Implement and register high-risk skills with explicit policy:
    - execute skill attempts when input/target is structurally valid
    - derive `unsafe` and related failure outcomes from attempted execution signals and post-condition evidence
    - do not broad pre-block risky actions globally; only reject hard-invalid or impossible requests up front
    - preserve never-throw contract and compact diagnostics for planner decisions.
  </action>
  <verify>
    <automated>cd /Users/aviyadava/minecraft-bot && npm run typecheck && npx tsx src/executor/UnsafePolicy.smoke.test.ts</automated>
  </verify>
  <done>High-risk skills are wired and unsafe outcomes are encoded from attempted execution evidence rather than blanket pre-blocking</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Encode deterministic unsafe and failure precedence mapping</name>
  <files>src/executor/failureMapping.ts</files>
  <action>
    Finalize failure mapping logic for attempted execution outcomes:
    - add explicit precedence for unsafe-vs-blocked-vs-timeout outcomes from real attempt signals
    - ensure identical attempted conditions map to identical ExecutorErrorCode values
    - keep mapping surface constrained to allowed codes and planner-usable metadata only.
  </action>
  <verify>
    <automated>cd /Users/aviyadava/minecraft-bot && npm run typecheck && npx tsx src/executor/UnsafePolicy.smoke.test.ts</automated>
  </verify>
  <done>Failure mapping deterministically represents unsafe semantics derived from attempted execution outcomes</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add fast smoke checks for unsafe policy and runtime stability</name>
  <files>src/executor/UnsafePolicy.smoke.test.ts, src/executor/SkillRuntime.smoke.test.ts</files>
  <action>
    Add quick-running smoke tests that validate:
    - unsafe classification is produced by attempted execution outcomes, not broad pre-check bans
    - structured result invariants hold for representative success/failure paths
    - movement coordinator behavior remains stable when these skills run concurrently.
  </action>
  <verify>
    <automated>cd /Users/aviyadava/minecraft-bot && npx tsx src/executor/UnsafePolicy.smoke.test.ts && npx tsx src/executor/SkillRuntime.smoke.test.ts</automated>
  </verify>
  <done>Fast smoke tests provide per-task signal while preserving full-suite checks for wave gate</done>
</task>

</tasks>

<verification>
Run Wave 3 checks:

```bash
cd /Users/aviyadava/minecraft-bot
npm run typecheck
npm run lint
npx tsx src/executor/UnsafePolicy.smoke.test.ts
npx tsx src/executor/SkillRuntime.smoke.test.ts
```

Verify unsafe outcomes are generated from attempted execution evidence and not from broad global pre-block policy.
</verification>

<success_criteria>
- `EXEC-01` high-risk skill subset is implemented and registered through executor
- `EXEC-02` unsafe/failure semantics are deterministic and derived from attempted execution outcomes
- `EXEC-03` movement mutex guarantees remain intact during non-movement execution
</success_criteria>

<output>
After completion, create `/Users/aviyadava/minecraft-bot/.planning/phases/04-skills-and-executor/04-03-SUMMARY.md`.
</output>
