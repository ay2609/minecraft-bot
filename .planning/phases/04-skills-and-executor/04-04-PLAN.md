---
phase: 04-skills-and-executor
plan: 04
type: execute
wave: 4
depends_on:
  - 04-03
files_modified:
  - src/executor/skills/craftItem.ts
  - src/executor/skills/dropItem.ts
  - src/executor/skills/equipItem.ts
  - src/executor/FailureCodeMatrix.test.ts
  - src/executor/SkillRuntime.integration.test.ts
  - src/executor/ExecutorLive.integration.test.ts
autonomous: false
requirements:
  - EXEC-01
  - EXEC-02
  - EXEC-03

must_haves:
  truths:
    - "All remaining required skills are executable through executor and return structured ExecutorResult outcomes"
    - "Full required ExecutorErrorCode coverage is validated with deterministic mapping expectations"
    - "Per-skill timeout budgets are enforced across the complete skill set"
    - "Movement mutex guarantees remain preserved under full-skill integration runs"
  artifacts:
    - path: "src/executor/skills/craftItem.ts"
      provides: "Recipe/material-aware crafting execution with structured failures"
      contains: "executeCraftItem"
    - path: "src/executor/skills/equipItem.ts"
      provides: "Inventory equipment transitions with deterministic result semantics"
      contains: "executeEquipItem"
    - path: "src/executor/FailureCodeMatrix.test.ts"
      provides: "Matrix coverage across required executor error codes"
      contains: "route_blocked"
    - path: "src/executor/ExecutorLive.integration.test.ts"
      provides: "Phase-level integration evidence across mixed skill classes"
      contains: "ExecutorLive"
  key_links:
    - from: "src/executor/skills/craftItem.ts"
      to: "src/executor/Executor.ts"
      via: "executed through shared timeout + normalization boundary"
      pattern: "execute"
    - from: "src/executor/Executor.ts"
      to: "src/executor/MovementCoordinator.ts"
      via: "retains movement mutex while full skill suite executes"
      pattern: "movementCoordinator"
---

<objective>
Finish Phase 04 by implementing remaining inventory-focused skills and running full integration gates for EXEC-01/EXEC-02/EXEC-03.

Purpose: This completes end-to-end executor coverage after Wave 3 unsafe-policy and high-risk skill work.
Output: Remaining skill implementations, full error-code matrix evidence, and phase gate completion.
</objective>

<execution_context>
@/Users/aviyadava/.codex/get-shit-done/workflows/execute-plan.md
@/Users/aviyadava/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/aviyadava/minecraft-bot/.planning/ROADMAP.md
@/Users/aviyadava/minecraft-bot/.planning/REQUIREMENTS.md
@/Users/aviyadava/minecraft-bot/.planning/STATE.md
@/Users/aviyadava/minecraft-bot/.planning/phases/04-skills-and-executor/04-CONTEXT.md
@/Users/aviyadava/minecraft-bot/.planning/phases/04-skills-and-executor/04-RESEARCH.md
@/Users/aviyadava/minecraft-bot/.planning/phases/04-skills-and-executor/04-VALIDATION.md
@/Users/aviyadava/minecraft-bot/.planning/phases/04-skills-and-executor/04-01-PLAN.md
@/Users/aviyadava/minecraft-bot/.planning/phases/04-skills-and-executor/04-02-PLAN.md
@/Users/aviyadava/minecraft-bot/.planning/phases/04-skills-and-executor/04-03-PLAN.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement remaining inventory-oriented skills</name>
  <files>src/executor/skills/craftItem.ts, src/executor/skills/dropItem.ts, src/executor/skills/equipItem.ts</files>
  <action>
    Implement remaining required skills with executor contract compliance:
    - validate inputs and inventory prerequisites deterministically
    - enforce timeout-aware execution and structured result returns
    - preserve compact planner-usable diagnostics with no uncaught throws.
  </action>
  <verify>
    <automated>cd /Users/aviyadava/minecraft-bot && npm run typecheck</automated>
  </verify>
  <done>Remaining required skills are implemented and executable through the typed executor boundary</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Complete full error-code and timeout matrix validation</name>
  <files>src/executor/FailureCodeMatrix.test.ts, src/executor/SkillRuntime.integration.test.ts</files>
  <action>
    Finalize broad EXEC-02 validation:
    - prove required error-code reachability and deterministic precedence
    - confirm timeout behavior remains consistent across all skill classes
    - ensure unsafe semantics from Wave 3 remain encoded in full matrix coverage.
  </action>
  <verify>
    <automated>cd /Users/aviyadava/minecraft-bot && npx tsx src/executor/FailureCodeMatrix.test.ts</automated>
  </verify>
  <done>Matrix validation confirms deterministic error-code and timeout semantics across complete skill runtime</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add phase-level mixed-skill integration coverage</name>
  <files>src/executor/ExecutorLive.integration.test.ts</files>
  <action>
    Extend end-to-end integration checks to cover representative successful and failed executions across movement + non-movement + inventory skills while preserving movement-mutex behavior.
  </action>
  <verify>
    <automated>cd /Users/aviyadava/minecraft-bot && npx tsx src/executor/ExecutorLive.integration.test.ts</automated>
  </verify>
  <done>Phase-level integration evidence demonstrates EXEC-01/EXEC-02 completeness with EXEC-03 preserved</done>
</task>

</tasks>

<verification>
Run Wave 4 full gate checks:

```bash
cd /Users/aviyadava/minecraft-bot
npm run typecheck
npm run lint
npm run build
npx tsx src/executor/FailureCodeMatrix.test.ts
npx tsx src/executor/SkillRuntime.integration.test.ts
npx tsx src/executor/ExecutorLive.integration.test.ts
npx tsx src/executor/**/*.test.ts
```

Manual runtime validation:
1. Execute scripted scenarios to trigger each required error code once.
2. Confirm all outcomes are structured `ExecutorResult` objects.
3. Re-run conflicting movement scenario to verify anti-oscillation remains intact.
</verification>

<success_criteria>
- `EXEC-01` all 10 skills are implemented and executable through executor
- `EXEC-02` structured outcome, timeout, and full error-code guarantees are validated
- `EXEC-03` movement mutex guarantees remain preserved across full-skill integration
</success_criteria>

<output>
After completion, create `/Users/aviyadava/minecraft-bot/.planning/phases/04-skills-and-executor/04-04-SUMMARY.md`.
</output>
