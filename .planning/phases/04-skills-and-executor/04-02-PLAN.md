---
phase: 04-skills-and-executor
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - src/executor/MovementCoordinator.ts
  - src/executor/skills/moveTo.ts
  - src/executor/skills/followEntity.ts
  - src/executor/Executor.ts
  - src/executor/types.ts
  - src/config.ts
  - src/executor/MovementCoordinator.test.ts
  - src/executor/ExecutorMovement.integration.test.ts
autonomous: true
requirements:
  - EXEC-02
  - EXEC-03

must_haves:
  truths:
    - "All movement pathing requests are serialized through a dedicated coordinator with at most one in-flight and one pending request"
    - "Concurrent movement conflicts produce deterministic queue, preemption, interruption, or stale-drop outcomes without oscillation"
    - "Movement-related outcomes remain in structured ExecutorResult format and honor timeout policy"
  artifacts:
    - path: "src/executor/MovementCoordinator.ts"
      provides: "Mutex arbitration for pathfinder ownership and pending movement queue policy"
      contains: "requestMove"
    - path: "src/executor/skills/moveTo.ts"
      provides: "move_to skill routed exclusively through movement coordinator"
      contains: "executeMoveTo"
    - path: "src/executor/skills/followEntity.ts"
      provides: "follow_entity pathing integrated with same movement mutex"
      contains: "executeFollowEntity"
    - path: "src/executor/MovementCoordinator.test.ts"
      provides: "Concurrency coverage for lock, preemption, pending-slot, and stale-drop rules"
      contains: "stale pending"
  key_links:
    - from: "src/executor/Executor.ts"
      to: "src/executor/MovementCoordinator.ts"
      via: "delegates movement skill execution through coordinator-owned pathfinder control"
      pattern: "movementCoordinator"
    - from: "src/executor/MovementCoordinator.ts"
      to: "src/events/EventBus.ts"
      via: "emits structured movement arbitration outcomes"
      pattern: "executor:result"
---

<objective>
Deliver movement mutex guarantees for Phase 04 by introducing a dedicated coordinator that prevents pathfinder oscillation under concurrent movement requests.

Purpose: This closes EXEC-03 and stabilizes `move_to`/`follow_entity` behavior before implementing remaining skills.
Output: Movement coordinator, movement skill integration, anti-oscillation tests, and stable handoff into split Wave 3/Wave 4 skill completion plans.
</objective>

<execution_context>
@/Users/aviyadava/.codex/get-shit-done/workflows/execute-plan.md
@/Users/aviyadava/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/aviyadava/minecraft-bot/.planning/ROADMAP.md
@/Users/aviyadava/minecraft-bot/.planning/REQUIREMENTS.md
@/Users/aviyadava/minecraft-bot/.planning/STATE.md
@/Users/aviyadava/minecraft-bot/.planning/phases/04-skills-and-executor/04-CONTEXT.md
@/Users/aviyadava/minecraft-bot/.planning/phases/04-skills-and-executor/04-RESEARCH.md
@/Users/aviyadava/minecraft-bot/.planning/phases/04-skills-and-executor/04-VALIDATION.md
@/Users/aviyadava/minecraft-bot/.planning/phases/04-skills-and-executor/04-01-PLAN.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement movement coordinator mutex and arbitration policy</name>
  <files>src/executor/MovementCoordinator.ts, src/executor/types.ts, src/config.ts</files>
  <action>
    Implement a dedicated movement coordinator that owns pathfinder transitions:
    - enforce one active movement operation and max one pending request
    - support intent-based critical preemption for safety-priority movement
    - auto-drop stale pending requests after configured TTL
    - return structured outcomes for queued, preempted, interrupted, dropped, and timed-out paths
    - avoid direct multi-writer `setGoal` usage outside coordinator.
  </action>
  <verify>
    <automated>cd /Users/aviyadava/minecraft-bot && npm run typecheck</automated>
  </verify>
  <done>Coordinator enforces deterministic movement ownership and anti-oscillation arbitration semantics</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Route move_to and follow_entity through movement coordinator</name>
  <files>src/executor/skills/moveTo.ts, src/executor/skills/followEntity.ts, src/executor/Executor.ts</files>
  <action>
    Integrate movement skills with coordinator:
    - convert both skills to coordinator-mediated requests only
    - preserve timeout enforcement and structured ExecutorResult semantics
    - map pathfinder cancellation and target-loss cases to deterministic error codes
    - ensure movement conflict outcomes are propagated via `executor:result` for planner visibility.
  </action>
  <verify>
    <automated>cd /Users/aviyadava/minecraft-bot && npm run typecheck && npm run lint</automated>
  </verify>
  <done>Movement skills share one arbitration path and no longer contend directly for pathfinder ownership</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add mutex and anti-oscillation integration tests</name>
  <files>src/executor/MovementCoordinator.test.ts, src/executor/ExecutorMovement.integration.test.ts</files>
  <action>
    Add tests validating EXEC-03 behavior:
    - second `move_to` during in-flight movement queues or preempts according to policy
    - only one pending movement request is retained
    - stale pending requests are dropped with explicit structured outcome
    - conflicting movement requests do not cause oscillating goal churn.
  </action>
  <verify>
    <automated>cd /Users/aviyadava/minecraft-bot && npm run typecheck && npm run lint && npx tsx src/executor/MovementCoordinator.test.ts && npx tsx src/executor/ExecutorMovement.integration.test.ts</automated>
  </verify>
  <done>Automated tests prove movement mutex correctness and anti-oscillation guarantees under concurrency</done>
</task>

</tasks>

<verification>
Run Wave 2 checks:

```bash
cd /Users/aviyadava/minecraft-bot
npm run typecheck
npm run lint
npx tsx src/executor/MovementCoordinator.test.ts
npx tsx src/executor/ExecutorMovement.integration.test.ts
```

Manual spot-check:
1. Trigger overlapping `move_to` commands while traversing.
2. Confirm a single coherent active path and explicit structured arbitration outcomes.
</verification>

<success_criteria>
- `EXEC-03` movement mutex behavior is implemented through a dedicated coordinator
- `move_to` and `follow_entity` no longer produce pathfinder contention/oscillation
- Movement arbitration is observable through structured executor outcomes
</success_criteria>

<output>
After completion, create `/Users/aviyadava/minecraft-bot/.planning/phases/04-skills-and-executor/04-02-SUMMARY.md`.
</output>
