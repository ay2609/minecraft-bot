---
phase: 04-skills-and-executor
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/executor/Executor.ts
  - src/executor/SkillRegistry.ts
  - src/executor/failureMapping.ts
  - src/executor/types.ts
  - src/config.ts
  - src/types/index.ts
  - src/executor/ExecutorResult.contract.test.ts
  - src/executor/ExecutorRetryPolicy.test.ts
  - src/executor/SkillRegistry.test.ts
autonomous: true
requirements:
  - EXEC-01
  - EXEC-02

must_haves:
  truths:
    - "For every valid executor invocation, exactly one `ExecutorResult` is returned/emitted and no uncaught throw escapes to callers"
    - "When an unregistered skill is requested, executor returns `invalid_state` and emits that outcome without crashing"
    - "When a skill call fails once, executor emits one mapped failure outcome with no automatic retry attempts by default"
    - "Configured per-skill timeout budgets deterministically produce `timed_out` outcomes when exceeded"
  artifacts:
    - path: "src/executor/Executor.ts"
      provides: "Timeout-guarded execution lifecycle and result normalization"
      contains: "execute"
    - path: "src/executor/SkillRegistry.ts"
      provides: "Canonical mapping from skill names to handlers"
      contains: "resolveSkill"
    - path: "src/executor/failureMapping.ts"
      provides: "Deterministic runtime-condition to ExecutorErrorCode mapping"
      contains: "mapExecutorFailure"
    - path: "src/executor/ExecutorResult.contract.test.ts"
      provides: "Contract tests for never-throw and ExecutorResult invariants"
      contains: "never throws"
    - path: "src/executor/ExecutorRetryPolicy.test.ts"
      provides: "Default retry-policy contract test proving no implicit retry loop"
      contains: "no auto-retry"
  key_links:
    - from: "src/executor/Executor.ts"
      to: "src/events/EventBus.ts"
      via: "emits normalized execution outcomes for downstream consumers"
      pattern: "eventBus.emit('executor:result'"
    - from: "src/config.ts"
      to: "src/executor/Executor.ts"
      via: "supplies per-skill timeout and executor behavior knobs"
      pattern: "executor"
---

<objective>
Establish Phase 04 executor foundations: a typed, never-throw executor runtime with centralized skill registry, timeout policy, and deterministic failure normalization.

Purpose: This provides the hard contract surface required before movement arbitration and full skill implementations.
Output: Executor core modules, config wiring, and contract tests validating EXEC-01/EXEC-02 baseline behavior.
</objective>

<execution_context>
@/Users/aviyadava/.codex/get-shit-done/workflows/execute-plan.md
@/Users/aviyadava/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/aviyadava/minecraft-bot/.planning/ROADMAP.md
@/Users/aviyadava/minecraft-bot/.planning/REQUIREMENTS.md
@/Users/aviyadava/minecraft-bot/.planning/STATE.md
@/Users/aviyadava/minecraft-bot/.planning/phases/04-skills-and-executor/04-CONTEXT.md
@/Users/aviyadava/minecraft-bot/.planning/phases/04-skills-and-executor/04-RESEARCH.md
@/Users/aviyadava/minecraft-bot/.planning/phases/04-skills-and-executor/04-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement executor runtime boundary with timeout and normalization wrapper</name>
  <files>src/executor/Executor.ts, src/executor/types.ts, src/config.ts, src/types/index.ts</files>
  <action>
    Build a typed executor entrypoint that accepts an ActionItem and always resolves to ExecutorResult:
    - enforce per-skill timeout budgets from config
    - normalize all thrown/rejected skill outcomes into valid ExecutorErrorCode values
    - execute exactly one attempt per action by default (no automatic retry unless explicitly configured later)
    - guarantee consistent duration and metadata fields
    - emit `executor:result` for both success and failure outcomes
    - return `invalid_state` for unknown or malformed actions instead of throwing.
  </action>
  <verify>
    <automated>cd /Users/aviyadava/minecraft-bot && npm run typecheck</automated>
  </verify>
  <done>Executor runtime guarantees never-throw structured results, single-attempt default execution, and timeout-controlled outcomes</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add canonical skill registry and deterministic failure mapper</name>
  <files>src/executor/SkillRegistry.ts, src/executor/failureMapping.ts, src/executor/types.ts</files>
  <action>
    Implement registry and failure mapping infrastructure:
    - register all 10 required skill names with typed handler signatures
    - implement shared runtime-failure precedence mapping for allowed error codes
    - ensure registry lookup failures produce structured `invalid_state` results
    - keep diagnostics compact and planner-usable (no raw stack/world dumps).
  </action>
  <verify>
    <automated>cd /Users/aviyadava/minecraft-bot && npm run typecheck && npm run lint</automated>
  </verify>
  <done>Registry and mapper enforce a single deterministic contract for skill dispatch and error-code semantics</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add contract tests for ExecutorResult invariants, retry policy, and skill coverage registration</name>
  <files>src/executor/ExecutorResult.contract.test.ts, src/executor/ExecutorRetryPolicy.test.ts, src/executor/SkillRegistry.test.ts</files>
  <action>
    Add tests that verify:
    - executor wrapper never throws across representative failure paths
    - every outcome satisfies ExecutorResult invariants (`success`/`errorCode` coherence)
    - default executor policy performs no automatic retry after a failed skill attempt
    - all 10 required skills are present and resolvable via registry
    - unknown skill handling returns `invalid_state` through structured result flow.
  </action>
  <verify>
    <automated>cd /Users/aviyadava/minecraft-bot && npm run typecheck && npm run lint && npx tsx src/executor/ExecutorResult.contract.test.ts && npx tsx src/executor/ExecutorRetryPolicy.test.ts && npx tsx src/executor/SkillRegistry.test.ts</automated>
  </verify>
  <done>Automated contract tests prove EXEC-01 registration completeness plus EXEC-02 never-throw and no-auto-retry-by-default guarantees</done>
</task>

</tasks>

<verification>
Run Wave 1 checks:

```bash
cd /Users/aviyadava/minecraft-bot
npm run typecheck
npm run lint
npx tsx src/executor/ExecutorResult.contract.test.ts
npx tsx src/executor/ExecutorRetryPolicy.test.ts
npx tsx src/executor/SkillRegistry.test.ts
```

Verify that all 10 skill names resolve from registry, executor never propagates uncaught errors, and failed actions are not auto-retried by default.
</verification>

<success_criteria>
- Executor core entrypoint is implemented with stable never-throw semantics
- Skill registry contains all required EXEC-01 skill identifiers
- Timeout and failure normalization policies satisfy EXEC-02 baseline constraints
</success_criteria>

<output>
After completion, create `/Users/aviyadava/minecraft-bot/.planning/phases/04-skills-and-executor/04-01-SUMMARY.md`.
</output>
